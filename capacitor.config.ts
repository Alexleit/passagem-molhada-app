import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.geophocus.passagemmolhada',
  appName: 'Vistoria Passagem Molhada',
  webDir: 'www'
};

export default config;
